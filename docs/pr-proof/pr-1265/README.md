# PR 1265 Visual Proof

Surface: avatar fallback rendering in profile and investor profile UI.

Proof captured from a temporary local Playwright fixture that mirrors the fallback sizes and centering rules added by the PR.

Screenshots:

- `avatar-fallback-centering.png` - small, medium, and large circular fallback containers with SVG and initials centered.
